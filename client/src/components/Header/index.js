import React from 'react';

const Header = () => {
    return (
        <header class="my-3">
            <h1 class="text-center">habit tracker.</h1>
        </header>
    );
};

export default Header;